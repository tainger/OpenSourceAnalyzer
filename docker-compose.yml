version: '3.8'

services:
  chroma:
    image: chromadb/chroma:latest
    container_name: ai-analyzer-chroma
    ports:
      - "8000:8000"
    volumes:
      - chroma_data:/chroma/chroma
    restart: unless-stopped

volumes:
  chroma_data:
